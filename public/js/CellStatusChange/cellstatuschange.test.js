describe("Given the method cellStatusChange from cellStatusChange", () => {
  describe("when we introduce the parameter 'Grid' it should", () => {
    test("return ", () => {
      const expectedValue = "1";

      const output = "1";
      expect(output).toStrictEqual(expectedValue);
    });
  });
});
